extends layout 

block content
  a(href="/categories/category/create", class="my-4 p-3 block w-fit bg-black text-white rounded font-semibold") Create Category
  h2(class="mb-4 text-3xl md:text-4xl font-bold") List of available Categories: 
  ul(class="grid grid-cols-1 md:grid-cols-3 gap-4") 
    each category in categories 
      li(class="flex border border-black rounded text-center font-semibold hover:bg-black hover:text-center hover:text-white")
        a(href=category.url, class="p-2 grow")  #{category.name}