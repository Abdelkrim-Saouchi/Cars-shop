extends layout 

block content 
  a(href="/cars/car/create", class="my-4 p-3 block w-fit bg-black text-white rounded font-semibold") Create Car 
  h2(class="mb-4 text-3xl md:text-4xl font-bold") List of available Cars: 
  ul(class="mb-4 grid grid-cols-1 md:grid-cols-3 gap-4")
    each car in cars
      li(class="border border-black rounded overflow") 
        img(src=(car.imgUrl === undefined ? "/images/car-placeholder.jpg" : car.imgUrl), alt="Car")
        div(class="p-2 flex flex-col gap-3 items-center")
          h3(class="text-2xl md:text-3xl") #{car.name} 
          p Price: #{car.price.toLocaleString('en-US')}$
          a(href=car.url, class="p-2 bg-black text-white border border-black rounded hover:bg-white hover:text-black") Explore